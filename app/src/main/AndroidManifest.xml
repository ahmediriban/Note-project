<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.notes_project_master_android">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_lancher_note"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_lancher_note"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

         <activity
             android:name=".ChooseCategoryStyleActivity">
         </activity>
         <activity
             android:name=".AddNewNoteActivity" />
        <activity
            android:name=".NoteBooksShowAll"
            android:launchMode="singleTask" />
        <activity
            android:name=".RegisterActivity"
            android:launchMode="singleTask" />
        <activity
            android:name=".LoginActivity"
            android:launchMode="singleTask" />
        <activity
            android:name=".ForgetPassActivity"
            android:launchMode="singleTask" />
        <activity
            android:name=".SplashActivity"
            android:launchMode="singleTask" />
        <activity
            android:name=".NotesShowAll"
            android:launchMode="singleTask" />
        <activity android:name=".EditNoteActivity" />
        <activity android:name=".HomeActivity"></activity>
        <activity android:name=".NoteTutorial2" />
        <activity android:name=".AddNewCategoryActivity" />
        <activity android:name=".GoToEmail" />
        <activity
            android:name=".NoteTutorial1"
            android:label="@string/app_name"
            android:launchMode="singleTask"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>